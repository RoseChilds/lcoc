const config = {
  title: "London College of Comedy",
  abr: "LCoC",
  enrolError: {
    title: "Uh oh",
    description: "This college isn't real, so you can't actually apply!",
    status: 'error',
    isClosable: true
  }
}

export default config;
